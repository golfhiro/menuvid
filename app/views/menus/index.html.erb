<%# 献立一覧の親要素を追加 %>
<div class="p-4">
  <div class="flex justify-center mb-4">
    <h1 class="text-4xl font-bold">今週の献立</h1>
  </div>

  <% if @menus.present? %>
    <%= render "menu_present", menus: @menus %>
  <% else %>
    <%= render "not_present", menus: @menus %>
  <% end %>

</div>
